export interface reservation {
  id: number;
  firstName: string;
  lastName: string;
  telephoneNumber: string;
  email: string;
  startDate: string;
  endDate: string;
  reservationCreation: string;
}
